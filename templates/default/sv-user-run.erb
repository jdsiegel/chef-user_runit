#!/bin/bash
#
# Run the service under a login shell so that sub-services will get the user's env. Useful for rvm/rbenv
exec 2>&1 sudo -H -u <%= @options['user'] %> bash -l -c "runsvdir -P /home/<%= @options['user'] %>/service 'log: ........................................................................................................................................................................................................................................................................................................................................'"
